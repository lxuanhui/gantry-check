// Fully static: the whole app is prerendered at build time into `build/` and uploaded as
// Cloudflare Workers static assets. There is no server-side rendering at request time.
export const prerender = true;
