# gantry-check web UI

SvelteKit (Svelte 5, static adapter) front end for the ERP estimator. It is prerendered
into `build/` and served by the Cloudflare Worker as static assets; see the "Web UI"
section of the repository README for how to run and deploy it.
